# ── Backend ────────────────────────────────────────────────────────────
# Copy to `.env` and fill in when the real Supabase project is ready.
# Without this file the app falls back to the development project baked
# into src/lib/backend.ts, so a fresh clone still runs.
#
# The EXPO_PUBLIC_ prefix is required: Expo only inlines variables with it
# into the client bundle. A variable named without the prefix reads as
# undefined at runtime and silently falls back to the default.
#
# The anon (publishable) key is meant to ship in the client — row-level
# security is what governs access. It is not a secret.

EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=<anon / publishable key>

# ── Theo dõi sự cố (tuỳ chọn) ──────────────────────────────────────────
# Không đặt biến này thì app chạy y hệt như trước khi có Sentry: không một
# request nào rời máy. Đó là chủ ý — xem `src/lib/observability.ts`.
#
# DSN không phải bí mật (nó ship trong client, giống anon key), nhưng nó
# vẫn KHÔNG được commit: đặt ở `.env` cục bộ và ở EAS secret.
#
# Hai thứ còn lại KHÔNG bao giờ vào tệp này và không vào repo:
#   SENTRY_AUTH_TOKEN  — chỉ EAS secret / CI secret. Thiếu nó thì sự cố
#                        native về không có tên hàm, chỉ là địa chỉ.
#   SENTRY_ORG / SENTRY_PROJECT — biến môi trường lúc dựng; config plugin
#                        đọc chúng khi app.json không khai.
EXPO_PUBLIC_SENTRY_DSN=<để trống nếu chưa bật>
