/**
 * Test-mode switches. Flip TEST_UNLOCK_ALL to false before release.
 *
 * While true:
 * - every mascot character is unlocked (including the "pro" ones)
 * - everything in the mascot-room shop is free (no coins deducted)
 * - unlock celebrations are suppressed (so 5 modals don't queue at once)
 */
export const TEST_UNLOCK_ALL = true;
